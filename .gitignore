# dependencies
/node_modules
.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage
/playwright-report
/test-results
/blob-report
/playwright/.cache

# env files — never commit real secrets
.env
.env*.local
.env.production

# drizzle
/drizzle/meta/_journal.json.bak

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# turbo / caches
.turbo
*.tsbuildinfo

# terraform (state holds secrets; lockfile IS committed)
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!*.tfvars.example

# graphify generated knowledge graph (regenerated by the post-commit hook)
graphify-out/

# Local backups — contain full user data (emails, password hashes, files).
# Never commit; keep host directory permissions tight (see docs/backups.md).
backups/

# Python virtualenv (AI plane local testing)
ai/.venv/
.venv/

# Python (the `ai` FastAPI service)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
